{
  "installCommand": "yarn install --frozen-lockfile",
  "buildCommand": "npx prisma generate --schema prisma/schema.prisma && npx next build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
