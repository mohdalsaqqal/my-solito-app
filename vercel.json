{
  "framework": "nextjs",
  "installCommand": "yarn install",
  "buildCommand": "cd apps/next && npx prisma generate && npx prisma migrate deploy && npx turbo run build --filter=next-app",
  "outputDirectory": "apps/next/.next"
}
