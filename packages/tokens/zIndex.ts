export const zIndex = {
  // Legacy aliases kept for compatibility while consumers migrate.
  base: 0,
  sticky: 500,
  searchTop: 800,
  toast: 1000,
  modal: 900,
  overlay: 800,
  header: 500,
  footer: 400,
  default: 0,
} as const;
